# FastAPI / rendering server configuration
# Copy this file to .env and adjust values as needed.

# Port the API server listens on
PORT=8000

# Comma-separated list of allowed CORS origins (use * for all)
CORS_ORIGINS=*
